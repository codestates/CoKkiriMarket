const fontSizes = {
};

const paddings = {
};

const margins = {
};

const colors = {
  white: '#FFFFFF',
  blue_dark: '#121D33',
  blue_normal: '#6793E6',
  blue_light: '#CCDFFF'
};

const theme = {
  fontSizes,
  paddings,
  margins,
  colors
};

export default theme;
